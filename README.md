## Setup / Installation

1. Set project root as working directory in terminal.
2. Run `echo >> .env 'exports.apiKey = "{API key here}";'`.
3. Run `bower install`.
4. Run `npm install`.
5. Run `gulp serve`.
